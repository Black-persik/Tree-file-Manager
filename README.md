# Tree Structure File Manager

## Описание проекта

Tree Structure File Manager - это консольное приложение на Java для визуализации древовидной структуры файловой системы, аналогичной используемой в UNIX-системах. Проект демонстрирует практическое применение структур данных и паттернов проектирования.

## Основные возможности

- **Визуализация дерева файлов** с отступами и символами для отображения иерархии
- **Поддержка файлов и директорий** с различными свойствами
- **Расчет общего размера** файловой системы с помощью Visitor паттерна
- **Эффективное хранение свойств файлов** с использованием Flyweight паттерна
- **Обход дерева** с помощью Iterator паттерна (DFS)

## Используемые паттерны проектирования

### 🎯 Visitor Pattern
- **Класс:** `SizeVisitor`
- **Назначение:** Расчет общего размера файловой системы
- **Реализация:** Посещение всех файлов и директорий для агрегации данных

### 🪶 Flyweight Pattern
- **Классы:** `FileProperties`, `FilePropertiesFactory`
- **Назначение:** Эффективное хранение общих свойств файлов
- **Преимущества:** Снижение потребления памяти за счет переиспользования объектов

### 🔄 Iterator Pattern
- **Класс:** `IterationInDepth`
- **Назначение:** Обход дерева файловой системы в глубину
- **Реализация:** Использование стека для DFS обхода

## Структура классов

### Основные классы:
- `Node` - базовый класс для всех узлов файловой системы
- `Directory` - представляет директорию с дочерними элементами
- `File` - представляет файл с размером и свойствами
- `FileProperties` - свойства файла (права доступа, владелец, группа)

### Вспомогательные классы:
- `Main` - точка входа в приложение
- `SizeVisitor` - вычисление общего размера
- `IterationInDepth` - итератор для обхода дерева
- `FilePropertiesFactory` - фабрика для создания свойств файлов

## Формат входных данных

Принимает команды двух типов:

### Создание директории:
- DIR <id> <parent_id> <name> # Директория с родителем
- DIR <id> <name> # Директория в корне

### Создание файла:
4
DIR 1 docs
DIR 2 1 images
FILE 1 user users 150.5 document.txt
FILE 2 user users 300 photo.jpg

### Выходные данные:
```bash
.
├── docs
│ ├── document.txt (150.5KB)
│ └── images
│ └── photo.jpg (300KB)
total: 450.5KB
```

## Запуск проекта

### Компиляция:
```bash
javac Main.java
```
